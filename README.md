# Deploy-Stand 🚀

**Модульная система для автоматизированного развертывания виртуальных машин в кластере Proxmox VE**

> **⚠️ ТЕСТОВАЯ ВЕТКА** - содержит экспериментальные функции и оптимизации архитектуры

[![Version](https://img.shields.io/badge/version-1.3.0--dev-orange.svg)](https://github.com/your-org/deploy-stand)
[![Python](https://img.shields.io/badge/python-3.8+-green.svg)](https://www.python.org/)
[![Proxmox](https://img.shields.io/badge/proxmox-7.0+-orange.svg)](https://www.proxmox.com/)

> **Полностью автоматизированная система развертывания** учебных стендов с автоматической перезагрузкой сети, умным управлением пользователями и гарантийной безопасностью данных.

## Описание

Deploy-Stand - это мощная модульная система, предназначенная для автоматизации процесса развертывания виртуальных машин в кластере Proxmox VE. Система построена на принципах модульной архитектуры, где каждый функциональный блок реализован как независимый модуль с четко определенными интерфейсами.

## Основные возможности

- � **Модульная архитектура** - каждый компонент системы является независимым модулем
- ⚖️ **Интеллектуальная балансировка нагрузки** - равномерное распределение виртуальных машин между нодами
- 🔧 **Гибкое управление шаблонами** - локальные шаблоны для оптимизации производительности
- 🌐 **Расширенная настройка сети** - поддержка сложных сетевых конфигураций
- 📊 **Мониторинг производительности** - сбор метрик и анализ производительности
- 💾 **Кеширование** - оптимизация часто используемых данных
- 🔐 **Безопасность** - поддержка аутентификации по паролю и токенам

## Архитектура системы

```
deploy-stand/
├── core/                          # Ядро системы
│   ├── interfaces/                # Интерфейсы модулей
│   ├── modules/                   # Реализации модулей
│   ├── config/                    # Конфигурация
│   ├── proxmox/                   # Proxmox API клиент
│   └── module_factory.py          # Фабрика модулей
├── ui/                            # Пользовательский интерфейс
│   └── cli/                       # Командная строка
├── utils/                         # Утилиты
│   ├── logging/                   # Логирование
│   ├── caching/                   # Кеширование
│   └── monitoring/                # Мониторинг
└── main.py                        # Точка входа
```

## Требования

- Python 3.8+
- Proxmox VE 7.0+
- Доступ к Proxmox API

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd deploy-stand
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Настройте конфигурацию подключения в `connections_config.yml`

## Быстрый старт

1. **Настройка подключения к Proxmox:**
```bash
python main.py
# Выберите пункт 7 "Управление подключением"
# Создайте новое подключение к вашему Proxmox кластеру
```

2. **Создание конфигурации развертывания:**
```bash
# Выберите пункт 1 "Создать конфигурацию развертывания"
# Выберите шаблон или создайте кастомную конфигурацию
```

3. **Указание пользователей:**
```bash
# Выберите пункт 3 "Указать список пользователей"
# Введите пользователей в формате user@pve
```

4. **Развертывание виртуальных машин:**
```bash
# Выберите пункт 4 "Развернуть конфигурацию"
# Выберите стратегию развертывания
# Дождитесь завершения процесса
```

## Конфигурационные файлы

### deployment_config.yml
```yaml
machines:
  - device_type: "linux"           # Тип: linux или ecorouter
    name: "student-pc"             # Имя создаваемой машины
    template_node: "pve1"          # Нода где находится шаблон
    template_vmid: 100             # VMID шаблона-источника
    networks:
      - bridge: "vmbr0"            # Интернет
      - bridge: "lab-net"          # Локальная сеть
      - bridge: "**vmbr100**"      # Зарезервированный интерфейс
    full_clone: false              # false = linked clone, true = full clone
```

### users_list.yml
```yaml
users:
  - "student1@pve"
  - "student2@pve"
  - "student3@pve"
```

### connections_config.yml
```yaml
"prod-cluster":
  host: "192.168.1.100:8006"
  user: "root@pam"
  use_token: false
  password: "encrypted_password"

"dev-cluster":
  host: "192.168.1.101:8006"
  user: "admin@pve"
  use_token: true
  token_name: "deploy-script"
  token_value: "encrypted_token"
```

## Модули системы

### Модули развертывания
- **LocalDeployer** ✅ - локальное развертывание linked/full clone (завершен и протестирован)
- **RemoteDeployer** ✅ - развертывание с подготовкой шаблонов (завершен и протестирован)
- **BalancedDeployer** ✅ - сбалансированное распределение по нодам (завершен и протестирован)
- **SmartDeployer** ⚠️ - умное развертывание с учетом нагрузки (в разработке, не функционален)

### Модули поддержки
- **BridgeManager** ✅ - управление сетевыми bridge'ами (завершен)
- **UserManager** ✅ - управление пользователями и пулами (завершен)
- **ConfigManager** ✅ - управление конфигурацией (завершен)
- **ProxmoxClient** ✅ - клиент Proxmox API (завершен)

## Использование API

```python
from core.module_factory import ModuleFactory
from core.proxmox.proxmox_client import ProxmoxClient
from core.config.config_manager import ConfigManager

# Создание фабрики модулей
factory = ModuleFactory()

# Регистрация модулей развертывания (балансировщики больше не используются)
factory.register_deployment_module("local", LocalDeployer)
factory.register_deployment_module("remote", RemoteDeployer)
factory.register_deployment_module("balanced", BalancedDeployer)
factory.register_deployment_module("smart", SmartDeployer)

# Создание клиентов
proxmox = ProxmoxClient(host="192.168.1.100:8006", user="root@pam", password="password")
config_manager = ConfigManager()

# Загрузка конфигурации
config = config_manager.load_deployment_config()
users = config_manager.load_users()

# Создание модуля развертывания (все модули теперь независимы)
deployer = factory.create_deployment_module("local", proxmox_client=proxmox)

# Развертывание виртуальных машин
results = deployer.deploy_configuration(users, config)
print(f"Создано пользователей: {len(results)}")
```

## Важные изменения в v1.3.0

### ✅ Архитектурные улучшения:
- **Полностью независимые модули** - каждый модуль развертывания самодостаточен
- **Встроенная балансировка** - модули содержат собственную логику балансировки нагрузки
- **Удален неиспользуемый код** - очищена кодовая база от артефактов
- **Упрощенная фабрика модулей** - меньше зависимостей и регистраций

### ⚠️ Критические изменения:
- **Удалены внешние балансировщики** - SimpleBalancer и SmartBalancer больше не используются
- **Изменена регистрация модулей** - модули развертывания регистрируются напрямую
- **Обновлен интерфейс пользователя** - адаптирован для работы с независимыми модулями

## Горячие клавиши интерфейса

- `d` - быстрый переход к развертыванию
- `c` - быстрый переход к созданию конфигурации
- `u` - быстрый переход к управлению пользователями
- `x` - быстрый переход к очистке ресурсов

## Мониторинг и логирование

Система предоставляет подробное логирование всех операций:

- Информационные сообщения о прогрессе операций
- Предупреждения о потенциальных проблемах
- Ошибки с детальной информацией для диагностики
- Метрики производительности для анализа

## Безопасность

- Поддержка аутентификации по паролю и токенам API
- Шифрование конфиденциальных данных в конфигурационных файлах
- Аудит всех операций развертывания
- Безопасное хранение паролей пользователей

## Производительность

- Кеширование часто используемых данных
- Асинхронная обработка длительных операций
- Оптимизированные алгоритмы балансировки нагрузки
- Минимизация сетевого трафика через локальные шаблоны

## Расширение системы

Благодаря модульной архитектуре, систему легко расширять:

1. Создайте новый интерфейс в `core/interfaces/`
2. Реализуйте модуль в `core/modules/`
3. Зарегистрируйте модуль в фабрике
4. Добавьте конфигурацию в YAML файлы

## Лицензия

Этот проект лицензирован под MIT License - см. файл LICENSE для подробностей.

## Поддержка

При возникновении проблем или вопросов:

1. Проверьте логи системы для диагностики проблем
2. Убедитесь в корректности конфигурационных файлов
3. Проверьте доступность Proxmox API
4. Создайте issue в репозитории проекта

## Авторы

- Deploy-Stand Team

## Версия и планы разработки

**Текущая версия:** 1.3.0-dev (@test-branch)

### ✅ Реализованные функции (v1.3.0):
- **Полностью независимые модули развертывания** - каждый модуль самодостаточен
- **Встроенная балансировка нагрузки** - модули содержат собственную логику балансировки
- **Оптимизированная архитектура** - удален неиспользуемый код и зависимости
- **Четкое разделение ответственности** - модули не влияют друг на друга
- **Упрощенное сопровождение** - меньше файлов и зависимостей

### 📋 Статус модулей развертывания:
| Модуль | Статус | Описание | Готовность |
|--------|--------|----------|------------|
| **LocalDeployer** | ✅ Готов | Локальное развертывание на ноде с шаблонами | 100% |
| **RemoteDeployer** | ✅ Готов | Удаленное развертывание с подготовкой шаблонов | 100% |
| **BalancedDeployer** | ✅ Готов | Сбалансированное распределение по всем нодам | 100% |
| **SmartDeployer** | ⚠️ В разработке | Умное развертывание с учетом нагрузки | 0% (не функционален) |

### 🚀 Запланированные улучшения:
1. **SmartDeployer** - реализация интеллектуальной балансировки с учетом реальной нагрузки
2. **Расширенный мониторинг** - сбор детальной статистики производительности
3. **Автоматическое масштабирование** - динамическое управление ресурсами
4. **Web-интерфейс** - графический интерфейс управления
5. **API документация** - полная документация REST API

### 🔧 Архитектурные улучшения:
- **Микросервисная архитектура** - разделение на независимые сервисы
- **Контейнеризация** - поддержка Docker контейнеров
- **CI/CD pipeline** - автоматизированное тестирование и развертывание
- **Мониторинг состояния** - health checks и alerting
