# 🖥️ Deploy-Stand - Автоматизация развертывания VM в Proxmox

**Deploy-Stand** — это система автоматического развертывания виртуальных машин в кластере Proxmox, предназначенная для быстрого создания учебных стендов для студентов.

> ⚠️ **Внимание:** Скрипт не тестировался в AltPVE!

---

## 📋 Содержание

- [Возможности](#-возможности)
- [Требования](#-требования)
- [Установка](#-установка)
- [Быстрый старт](#-быстрый-старт)
- [Структура проекта](#-структура-проекта)
- [Подробное описание работы](#-подробное-описание-работы)
- [Конфигурационные файлы](#-конфигурационные-файлы)
- [Примеры использования](#-примеры-использования)
- [Сетевая конфигурация](#-сетевая-конфигурация)
- [Устранение неполадок](#-устранение-неполадок)
- [Благодарности](#-благодарности)

---

## 🚀 Возможности

### Основные функции

- ✅ **Автоматическое клонирование VM** из существующих шаблонов
- ✅ **Выбор типа клонирования**: Linked или Full clone
- ✅ **Массовое развертывание** стендов для множества пользователей
- ✅ **Балансировка нагрузки** между нодами кластера Proxmox
- ✅ **Гибкая сетевая топология** с автоматическим созданием мостов
- ✅ **Управление пулами и правами** пользователей
- ✅ **Поддержка различных типов устройств**: Linux и Ecorouter
- ✅ **Интерактивное CLI меню** для удобной работы

### Ключевые особенности

1. **Два типа клонирования VM**:
   - **Linked Clone** (по умолчанию) - быстрое создание, экономит дисковое пространство
   - **Full Clone** - полная копия, независимая от шаблона
2. **Равномерное распределение** стендов по нодам кластера
3. **Автоматическая генерация паролей** для пользователей
4. **Изолированные сети** для каждого пользователя
5. **Простое удаление** ресурсов пользователей

---

## 📦 Требования

### Системные требования

- **Python**: версия 3.8 или выше
- **Proxmox VE**: версия 6.x или 7.x
- **Операционная система**: Linux (тестировалось на Ubuntu/Debian)

### Необходимые права в Proxmox

Для работы скрипта требуется учетная запись с правами:
- Создание и управление VM
- Создание пулов (pools)
- Создание пользователей и настройка ACL
- Управление сетевыми интерфейсами

---

## 🔧 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/aleti000/deploy-stand.git
cd deploy-stand
```

### 2. Установка зависимостей

```bash
pip3 install -r requirements.txt
```

Будут установлены следующие пакеты:
- `proxmoxer>=1.3.0` - библиотека для работы с Proxmox API
- `PyYAML>=6.0` - для работы с YAML конфигурациями
- `requests>=2.31.0` - для HTTP запросов
- `colorama>=0.4.6` - для цветного вывода в терминале

---

## ⚡ Быстрый старт

### Запуск программы

```bash
python3 main.py
```

### Первое подключение

При первом запуске вам будет предложено ввести данные для подключения к Proxmox:

1. **Адрес сервера**: например, `192.168.1.100:8006`
2. **Имя пользователя**: например, `root@pam`
3. **Метод аутентификации**:
   - **Пароль**: стандартная аутентификация
   - **API Token**: более безопасный метод (рекомендуется)

### Основной рабочий процесс

1. **Создайте конфигурацию развертывания** (пункт меню 1)
2. **Укажите список пользователей** (пункт меню 2)
3. **Запустите развертывание** (пункт меню 3)
4. **Сохраните сгенерированные пароли** для передачи студентам

---

## 📁 Структура проекта

```
deploy-stand/
├── main.py                      # Точка входа в приложение
├── requirements.txt             # Зависимости Python
├── README.md                    # Документация (этот файл)
├── LICENSE                      # Лицензия проекта
├── .gitignore                   # Игнорируемые файлы для Git
│
├── app/                         # Основной код приложения
│   ├── __init__.py
│   │
│   ├── core/                    # Ядро системы
│   │   ├── config.py            # Управление конфигурацией
│   │   ├── proxmox_manager.py   # Работа с Proxmox API
│   │   ├── vm_deployer.py       # Логика развертывания VM
│   │   └── user_manager.py      # Управление пользователями
│   │
│   ├── ui/                      # Пользовательский интерфейс
│   │   └── cli_menus.py         # CLI меню
│   │
│   └── utils/                   # Вспомогательные утилиты
│       └── console.py           # Функции цветного вывода
│
└── data/                        # Конфигурационные файлы
    ├── deployment_config.yml    # Конфигурация развертывания
    └── users_list.yml           # Список пользователей
```

---

## 🔍 Подробное описание работы

### Модуль: config.py

**Назначение:** Управление конфигурационными файлами

**Функции:**
- `create_deployment_config()` - интерактивное создание конфигурации стенда
- `load_deployment_config()` - загрузка существующей конфигурации
- `save_users()` - сохранение списка пользователей
- `load_users()` - загрузка списка пользователей

### Модуль: proxmox_manager.py

**Назначение:** Взаимодействие с Proxmox API

**Основные методы:**
- `connect()` - подключение к Proxmox (пароль или токен)
- `get_nodes()` - получение списка нод кластера
- `clone_vm()` - клонирование виртуальной машины
- `ensure_bridge()` - создание сетевого моста при необходимости
- `get_next_vmid()` - получение следующего свободного VMID
- `delete_vm()` - удаление виртуальной машины
- `delete_bridge()` - удаление сетевого моста

### Модуль: vm_deployer.py

**Назначение:** Логика развертывания виртуальных машин

**Основные возможности:**
- Создание VM из шаблонов
- Настройка сетевых интерфейсов
- Балансировка между нодами
- Генерация MAC-адресов для Ecorouter
- Управление алиасами сетей

**Типы устройств:**
- **linux** - стандартные Linux VM (virtio сетевые адаптеры)
- **ecorouter** - роутеры Ecorouter (vmxnet3 адаптеры с управляющим портом)

### Модуль: user_manager.py

**Назначение:** Управление пользователями и их ресурсами

**Функции:**
- `create_user_and_pool()` - создание пользователя и пула
- `delete_user_resources()` - удаление всех ресурсов пользователя
- `_generate_password()` - генерация случайного пароля

**Схема прав:**
- Для каждого пользователя создается отдельный пул
- Пользователь получает роль `PVEVMAdmin` на свой пул
- Все VM пользователя добавляются в его пул

### Модуль: cli_menus.py

**Назначение:** Интерактивное CLI меню

**Функции меню:**
1. Создание конфигурации для развертывания
2. Указание списка пользователей
3. Развертывание конфигурации
4. Удаление машин по списку пользователей
5. Удаление машин отдельного пользователя
0. Выход

---

## ⚙️ Конфигурационные файлы

### deployment_config.yml

Описывает структуру развертываемого стенда.

**Пример конфигурации:**

```yaml
machines:
  - device_type: ecorouter          # Тип устройства
    name: eco                        # Имя VM
    template_node: SRV1-PVE         # Нода с шаблоном
    template_vmid: 100              # VMID шаблона
    networks:                        # Список сетевых интерфейсов
      - bridge: vmbr0               # Интернет (управляющий порт для ecorouter)
      - bridge: hq                  # Внутренняя сеть (алиас)
  
  - device_type: linux
    name: lin
    template_node: SRV1-PVE
    template_vmid: 101
    networks:
      - bridge: hq                  # Та же внутренняя сеть
```

**Параметры машины:**
- `device_type` - тип устройства (`linux` или `ecorouter`)
- `name` - имя создаваемой VM
- `template_node` - нода, где находится шаблон
- `template_vmid` - VMID шаблона для клонирования
- `networks` - список сетевых интерфейсов
- `full_clone` - тип клонирования (`true` для полной копии, `false` для связанного клона)

### users_list.yml

Список пользователей для развертывания.

**Пример:**

```yaml
users:
  - user1@pve
  - user2@pve
  - user3@pve
  - user4@pve
```

**Формат:** `username@realm`, где realm обычно `pve` или `pam`

---

## 💡 Примеры использования

### Пример 1: Простой стенд с одной машиной

**Задача:** Развернуть по одной Linux VM для 10 студентов.

**Конфигурация (deployment_config.yml):**
```yaml
machines:
  - device_type: linux
    name: student-vm
    template_node: pve1
    template_vmid: 200
    networks:
      - bridge: vmbr0
```

**Список пользователей (users_list.yml):**
```yaml
users:
  - student1@pve
  - student2@pve
  # ... до student10@pve
```

### Пример 2: Стенд с роутером и двумя хостами

**Задача:** Создать сеть с роутером и двумя хостами в изолированной сети.

**Конфигурация:**
```yaml
machines:
  - device_type: ecorouter
    name: router
    template_node: pve1
    template_vmid: 100
    networks:
      - bridge: vmbr0      # Интернет
      - bridge: lan        # Внутренняя сеть
  
  - device_type: linux
    name: host1
    template_node: pve1
    template_vmid: 101
    networks:
      - bridge: lan
  
  - device_type: linux
    name: host2
    template_node: pve1
    template_vmid: 101
    networks:
      - bridge: lan
```

### Пример 3: Сложная топология с несколькими сетями

**Задача:** Создать сеть с роутером, web-сервером в DMZ и клиентом в LAN.

**Конфигурация:**
```yaml
machines:
  - device_type: ecorouter
    name: gateway
    template_node: pve1
    template_vmid: 100
    networks:
      - bridge: vmbr0      # WAN (интернет)
      - bridge: dmz        # DMZ сеть
      - bridge: lan        # LAN сеть
  
  - device_type: linux
    name: web-server
    template_node: pve1
    template_vmid: 102
    networks:
      - bridge: dmz
  
  - device_type: linux
    name: client
    template_node: pve1
    template_vmid: 101
    networks:
      - bridge: lan
```

---

## 🌐 Сетевая конфигурация

### Принципы работы с сетями

1. **vmbr0** - зарезервирован для интернет-соединения (не создается автоматически)
2. **Алиасы сетей** - любое имя bridge, отличное от `vmbr0`, рассматривается как алиас
3. **Автоматические мосты** - создаются с именами `vmbr1000+`
4. **Изоляция пользователей** - каждый пользователь получает свой набор мостов

### Схема маппинга сетей

```
Конфигурация:         Реальные мосты:
─────────────────     ───────────────────
bridge: vmbr0    →    vmbr0 (существующий)
bridge: hq       →    vmbr1000 (user1)
bridge: hq       →    vmbr1001 (user2)
bridge: dmz      →    vmbr1002 (user1)
bridge: dmz      →    vmbr1003 (user2)
```

### Особенности Ecorouter

Для устройств типа `ecorouter`:
**Статистика разработки:**
- ⏱️ Время от идеи до релиза: ~5 часов
- 🤖 Использованный AI: Cursor
- 📝 Строк кода: ~1000+

---
- **net0** создается автоматически как управляющий порт (link_down=1)
- Используются **vmxnet3** адаптеры
- MAC-адреса генерируются в диапазоне `1C:87:76:40:XX:XX`
- Первая сеть в конфигурации обычно `vmbr0` (интернет)

### Важно: Перезагрузка сети

После создания новых мостов необходимо перезагрузить сеть на нодах:

```bash
systemctl restart networking
```

Или перезагрузить ноду:

```bash
reboot
```

---

## 🔧 Устранение неполадок

### Проблема: Не удается подключиться к Proxmox

**Причины и решения:**

1. **Неверные учетные данные**
   - Проверьте имя пользователя и пароль/токен
   - Убедитесь в правильности realm (@pam или @pve)

2. **Проблемы с сертификатом SSL**
   - Скрипт использует `verify_ssl=False`
   - Убедитесь, что Proxmox доступен по HTTPS

3. **Сетевая недоступность**
   - Проверьте доступность хоста: `ping proxmox-host`
   - Убедитесь, что порт 8006 открыт: `telnet proxmox-host 8006`

### Проблема: Ошибка при клонировании VM

**Возможные причины:**

1. **Нет места на storage**
   - Проверьте свободное место на хранилище
   - Используйте другой storage для шаблонов

2. **Шаблон не найден**
   - Убедитесь, что template_vmid существует на указанной ноде
   - Проверьте, что VM является шаблоном

3. **Конфликт VMID**
   - Скрипт автоматически ищет свободные VMID
   - Если проблема сохраняется, проверьте наличие дубликатов вручную

### Проблема: Сетевые адаптеры не работают

**Решение:**

1. Перезагрузите сеть на ноде:
   ```bash
   systemctl restart networking
   ```

2. Проверьте созданные мосты:
   ```bash
   ip link show | grep vmbr
   ```

3. Убедитесь, что мосты active:
   ```bash
   brctl show
   ```

### Проблема: Не удается удалить ресурсы пользователя

**Причины:**

1. **VM запущены**
   - Скрипт пытается остановить VM автоматически
   - При необходимости остановите вручную

2. **Мосты используются другими VM**
   - Скрипт проверяет использование перед удалением
   - Мосты не будут удалены, если используются

### Получение отладочной информации

Для получения дополнительной информации можно модифицировать код:

```python
# В начале файла proxmox_manager.py
import logging
logging.basicConfig(level=logging.DEBUG)
```

---

## 🎯 Рекомендации по использованию

### Подготовка шаблонов

1. **Создайте базовые VM** для каждого типа устройства
2. **Настройте cloud-init** (опционально) для автоматической конфигурации
3. **Конвертируйте в шаблоны** через веб-интерфейс Proxmox
4. **Протестируйте клонирование** вручную перед массовым развертыванием

### Выбор типа клонирования

При создании конфигурации развертывания теперь доступны два типа клонирования:

**Linked Clone (по умолчанию)**:
- ✅ Быстрое создание виртуальных машин
- ✅ Экономит дисковое пространство (использует copy-on-write)
- ✅ Зависит от оригинального шаблона
- ✅ Рекомендуется для большинства случаев

**Full Clone**:
- ✅ Полностью независимая копия шаблона
- ✅ Не зависит от оригинального шаблона
- ✅ Требует больше времени и места на диске
- ✅ Рекомендуется для продакшн-среды или долгосрочных стендов

### Планирование ресурсов

- **RAM**: Рассчитайте общий объем (VM × пользователи)
- **CPU**: Учитывайте overcommit (обычно 1:4 безопасно)
- **Storage**: Full clones требуют в 2-3 раза больше места, чем linked clones
- **Network**: Планируйте VLAN при необходимости

### Настройка хранилищ для клонирования между нодами

Для успешного клонирования шаблонов между разными нодами необходимо правильно настроить хранилища:

#### Способ 1: Настройка NFS хранилища (самый простой)

**На сервере NFS:**
```bash
# Установите NFS сервер
sudo apt update && sudo apt install nfs-kernel-server

# Создайте директорию для хранения
sudo mkdir -p /srv/nfs/proxmox-storage
sudo chown nobody:nogroup /srv/nfs/proxmox-storage
sudo chmod 777 /srv/nfs/proxmox-storage

# Добавьте в /etc/exports
sudo nano /etc/exports
# Добавьте строку:
/srv/nfs/proxmox-storage 192.168.1.0/24(rw,sync,no_subtree_check,no_root_squash)

# Перезапустите NFS
sudo systemctl restart nfs-kernel-server
sudo exportfs -a
```

**На всех нодах Proxmox:**
```bash
# Установите NFS клиент
sudo apt update && sudo apt install nfs-common

# Создайте точку монтирования
sudo mkdir -p /mnt/pve/shared-storage

# Добавьте в /etc/fstab (укажите IP вашего NFS сервера)
192.168.1.100:/srv/nfs/proxmox-storage /mnt/pve/shared-storage nfs defaults 0 0

# Монтируйте хранилище
sudo mount /mnt/pve/shared-storage
```

**В веб-интерфейсе Proxmox:**
1. Перейдите в: **Datacenter → Storage → Add → Directory**
2. Укажите:
   - **ID**: shared-storage
   - **Directory**: /mnt/pve/shared-storage
   - **Content**: Images, ISO, Backup, Container
   - **Nodes**: Выберите все ноды
   - **Enable**: Да

#### Способ 2: Настройка Ceph RBD

**На всех нодах кластера:**
```bash
# Установите Ceph
sudo apt update
sudo apt install ceph-common

# Создайте конфигурационный файл Ceph (укажите monitor ноды)
sudo nano /etc/ceph/ceph.conf
[global]
mon host = 192.168.1.10,192.168.1.11,192.168.1.12
```

**На монитор нодах:**
```bash
# Создайте pool для VM дисков
ceph osd pool create vm-storage 128
ceph auth get-or-create client.proxmox mon 'allow r' osd 'allow rwx pool=vm-storage'
ceph auth get-key client.proxmox | tee client.proxmox.key

# Разрешите application для RBD
ceph osd pool application enable vm-storage rbd
```

**В веб-интерфейсе Proxmox:**
1. **Datacenter → Storage → Add → RBD**
2. Укажите:
   - **ID**: ceph-storage
   - **Pool**: vm-storage
   - **Monitor Host**: IP монитор нод через запятую
   - **User Name**: proxmox
   - **Content**: Images, ISO, Backup, Container

#### Способ 3: Настройка локальных хранилищ с репликацией

**На каждой ноде создайте локальное хранилище:**
```bash
# Создайте директорию для хранения
sudo mkdir -p /var/lib/vz/images

# Добавьте хранилище через веб-интерфейс:
# Datacenter → Storage → Add → Directory
# ID: local-images-{nodename}
# Directory: /var/lib/vz/images
# Content: Images
```

**Копирование шаблонов между нодами:**
```bash
# Скопируйте шаблон на другую ноду
scp /var/lib/vz/images/template.qcow2 root@pve2:/var/lib/vz/images/
```

#### Проверка хранилищ

**Через командную строку:**
```bash
# Проверьте доступные хранилища
pvesm status

# Проверьте содержимое хранилища
pvesm list STORAGE_ID

# Проверьте статус хранилища
pvesm status -storage STORAGE_ID
```

**В веб-интерфейсе:**
- **Datacenter → Storage** - проверьте статус всех хранилищ
- Убедитесь, что хранилища активны и доступны на всех нодах
- Проверьте тип содержимого (должна быть поддержка `images`)

#### Рекомендации по настройке

1. **Для небольших кластеров** используйте NFS - проще в настройке
2. **Для продакшн-среды** рекомендуется Ceph - лучшая производительность и надежность
3. **Разместите часто используемые шаблоны** на всех нодах для ускорения развертывания
4. **Регулярно проверяйте** доступность хранилищ и свободное место
5. **Настройте мониторинг** хранилищ для предотвращения простоев

### Безопасность

- Используйте **API токены** вместо паролей
- Ограничьте **права токенов** только необходимым минимумом
- Регулярно **обновляйте пароли** пользователей
- Рассмотрите использование **firewall правил** в Proxmox

---

## 📚 Дополнительная информация

### API Token в Proxmox

Создание API токена (рекомендуемый метод):

1. Войдите в Proxmox VE
2. Datacenter → Permissions → API Tokens
3. Add → укажите User, Token ID
4. Снимите галочку "Privilege Separation" для полных прав
5. Сохраните Token Secret (показывается только один раз!)

### Стратегии размещения VM

**Specific Node** - все VM на одной ноде:
- Проще для управления
- Подходит для малых групп
- Риск перегрузки одной ноды

**Balanced** - равномерное распределение:
- Оптимальная загрузка кластера
- Для больших групп студентов
- Автоматическая балансировка

### Клонирование между нодами

Система теперь поддерживает клонирование шаблонов между разными нодами кластера:

**Автоматическое определение хранилища:**
- 🔍 Автоматический поиск общего хранилища между нодами
- 💾 Предпочтение хранилищам с поддержкой образов дисков (`images`)
- 🔄 Использование shared storage при отсутствии локального

**Рекомендации для клонирования между нодами:**
1. **Настройте shared storage** между нодами в Proxmox
2. **Используйте локальные хранилища** на каждой ноде для шаблонов
3. **Разместите часто используемые шаблоны** на всех нодах
4. **Проверьте доступность хранилищ** перед развертыванием

**Если клонирование между нодами не работает:**
- Убедитесь, что между нодами настроено общее хранилище
- Попробуйте разместить шаблон на целевой ноде
- Проверьте права доступа к хранилищам

### Ограничения текущей версии

- ⚠️ Поддерживаются только **linked clones**
- ⚠️ Не поддерживается **миграция VM** между нодами в процессе
- ⚠️ Нет автоматического **мониторинга** развернутых стендов
- ⚠️ Требуется ручная **перезагрузка сети** после создания мостов

---

## 🗺️ Планы развития

- [ ] Поддержка **full clones**
- [ ] Веб-интерфейс для управления
- [ ] Автоматический мониторинг стендов
- [ ] Шаблоны конфигураций для типовых задач
- [ ] Интеграция с системами управления обучением (LMS)
- [ ] Автоматическая очистка старых стендов
- [ ] Поддержка снимков (snapshots) VM
- [ ] Экспорт отчетов о развернутых стендах

---

## 🤝 Вклад в проект

Мы приветствуем любой вклад в развитие проекта!

### Как помочь:

1. **Fork** репозитория
2. Создайте **feature branch** (`git checkout -b feature/AmazingFeature`)
3. **Commit** изменения (`git commit -m 'Add some AmazingFeature'`)
4. **Push** в branch (`git push origin feature/AmazingFeature`)
5. Откройте **Pull Request**

### Сообщения об ошибках

Используйте [GitHub Issues](https://github.com/aleti000/deploy-stand/issues) для:
- Сообщений об ошибках
- Предложений новых функций
- Вопросов по использованию

---

## 📄 Лицензия

Этот проект распространяется под лицензией, указанной в файле [LICENSE](LICENSE).

---

## 📞 Контакты

- **GitHub**: [aleti000](https://github.com/aleti000)
- **Repository**: [deploy-stand](https://github.com/aleti000/deploy-stand)

---

## 🔗 Полезные ссылки

- [Документация Proxmox VE](https://pve.proxmox.com/pve-docs/)
- [Proxmoxer API Documentation](https://pypi.org/project/proxmoxer/)
- [Proxmox API Reference](https://pve.proxmox.com/pve-docs/api-viewer/)

---

<div align="center">

**Сделано с ❤️ для образования**

</div>
